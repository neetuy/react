import React from 'react'
import ReactDOM from 'react-dom';
import { Router, Route, Link, browserHistory, IndexRoute  } from 'react-router'

class RecatRouterL extends React.Component {
   render() {
      return (
        
            <h2>Lost context</h2>
         
      );
   }
}

export default RecatRouterL;
