import React from 'react';
import ReactDOM from 'react-dom';
import { Router, Route, Link, browserHistory, IndexRoute  } from 'react-router'

class ReactRouterEx extends React.Component {
   render() {
      return (
            <h2>Hi</h2>
      );
   }
}

export default ReactRouterEx;
